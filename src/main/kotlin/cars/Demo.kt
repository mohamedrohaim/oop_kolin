package cars

class Demo {
    // can not create an object from car class
    // so  will create an instance of classes that inherit from car class
    fun poly(car:Car){

        car.carInfo()
    }
}